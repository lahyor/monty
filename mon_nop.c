#include "monty.h"

/**
 * m_nop - do nothing
 * @head: pointer to pointer of first node
 * @count: line counter
 * Return: none
 */

void m_nop(stack_t **head, unsigned int count)
{
	(void) count;
	(void) head;
}
